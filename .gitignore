node_modules/
dist/
build/
out/
.turbo/
.next/

# All env files except a checked-in template (.env.example if you ever add one)
.env
.env.*
!.env.example

*.log
.DS_Store
Thumbs.db
coverage/
tmp/
.tsbuildinfo
*.tsbuildinfo

# Render artifacts — FFmpeg scratch dir, generated per project, can be GBs each
# Match both the leading-dot variant (`.render-work`) and the bare variant
# (`render-work`) that the worker creates at relative paths.
renders/
fx-cache/
.render-work/
**/.render-work/
render-work/
**/render-work/

# Diff / patch files often dropped by `git diff > x.patch` or review tooling
*.patch
*.diff

# Misc local dev junk
.DS_Store
.cache/
.review-diff.patch

# Vercel CLI local state
.vercel/

# Editor
.vscode/
.idea/
*.swp
*~
