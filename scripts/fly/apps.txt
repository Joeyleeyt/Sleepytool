# EmberForge Fly app. Each line:  <app-name>  <fly-toml>
# Used by scripts/fly/*.{sh,ps1} to drive deploys + secrets.
# The orchestrator + 69labs/veo3 workers now run as the `workers` process
# group and ffmpeg as the `render` group — both inside ONE app. See
# infra/fly/sleepytool.fly.toml. Web + API ship on Vercel.
sleepytool      infra/fly/sleepytool.fly.toml
