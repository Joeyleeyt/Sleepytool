# EmberForge Fly apps. Each line:  <app-name>  <fly-toml>
# Used by scripts/fly/*.{sh,ps1} to iterate over the fleet.
#
# CONSOLIDATED: all 5 pipeline processes (orchestrator + 4 workers) now run as
# process groups inside the single "sleepytool" app. See the [processes] block
# in infra/fly/sleepytool.fly.toml. Web + API ship separately on Vercel.
sleepytool      infra/fly/sleepytool.fly.toml
